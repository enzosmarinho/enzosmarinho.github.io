{
  "name": "enzosmarinho-portfolio",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check cases.js && node --check versions/kinetic/kinetic.js && node --check versions/kinetic/vortex.js && npm test",
    "package": "node scripts/package-public.mjs"
  }
}
