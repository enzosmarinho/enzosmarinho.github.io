{
  "name": "enzosmarinho-portfolio",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check cases.js && node --check versions/kinetic/kinetic.js && npm test"
  }
}
